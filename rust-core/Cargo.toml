[package]
name = "pyferox-rust"
version = "0.1.0"
edition = "2021"

[lib]
name = "pyferox_rust"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
serde_json = "1.0"
